<nav id="top-nav" class="navbar navbar-expand-lg bg-body-tertiary mb-3">
  <div class="container">
    <%= link_to 'Pousadaria', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item nav-link"><%= link_to 'Gestão de Pousadas', inn_management_path, class: 'navbar-link'  unless user_signed_in?  %></li>
        <li class="nav-item nav-link"><%= link_to 'Entrar como cliente', new_user_session_path, class: 'navbar-link'  unless user_signed_in? || inn_owner_signed_in? %></li>
        <% if inn_owner_signed_in? %>
         <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_inn_owner.email %>
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">Meu Perfil</a></li>
            <li><hr class="dropdown-divider"></li>
            
            <li><%= button_to 'Sair', destroy_inn_owner_session_path, { method: :delete,  class: 'dropdown-item', form: { "data-turbo": false } } if inn_owner_signed_in? %></li>
          </ul>
        </li>
        <% end %>
        <% if user_signed_in? %>
         <li class="nav-item dropdown">
          <li class="nav-item nav-link"><%= link_to 'Lista de favoritos', favorite_lists_path, class: 'navbar-link' %></li>
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.email %>
          </a>
          
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">Meu Perfil</a></li>
            <li><hr class="dropdown-divider"></li>
            
            <li><%= button_to 'Sair', destroy_user_session_path, { method: :delete,  class: 'dropdown-item', form: { "data-turbo": false } } if user_signed_in? %></li>
          </ul>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>